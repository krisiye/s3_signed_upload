'use strict';

describe('myApp.s3upload module', function() {

  beforeEach(module('myApp.s3upload'));

  describe('s3upload controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var s3uploadCtrl = $controller('s3uploadCtrl');
      expect(s3uploadCtrl).toBeDefined();
    }));

  });
});
